# pydantic<2.10: brak wheela pydantic-core dla Python 3.13 → pip próbuje budować z Rusta
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-chess==1.999
pydantic==2.11.7
python-dotenv==1.0.1

# --- CV (Computer Vision) ---
opencv-python==4.11.0.86
requests==2.32.3
numpy>=1.24.0

# --- ML modele ---
# torch i torchvision: CPU-only build (mniejszy download, wystarczy do klasyfikacji)
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0        # YOLOv8 do detekcji szachownicy
albumentations>=1.3.0     # augmentacja danych treningowych
onnxruntime>=1.17.0       # szybsza inferencja CNN na CPU
onnx>=1.14.0              # potrzebne do eksportu modelu PyTorch → ONNX
