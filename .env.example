# Skopiuj do .env w katalogu głównym repozytorium (plik .env jest w .gitignore).
# Domyślnie backend używa: <repo>/stockfish/stockfish-windows-x86-64-avx2.exe
# Odkomentuj i ustaw tylko, gdy binarka jest gdzie indziej lub ma inną nazwę:
# STOCKFISH_PATH=C:\stockfish\stockfish.exe
